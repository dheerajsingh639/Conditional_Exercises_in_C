#include <stdio.h>
int main()
{
    int m, n = 1;
    printf("enter m\n");
    scanf("%d", &m);
    if (m != 0)
    {
        if (m = 0)
            n = 1;
        else
            n = -1;
    }
    else
    {
        n = 0;
    }
    printf("the value of m is %d\n", m);
    printf("the value of n is %d\n", n);

    return 0;
}